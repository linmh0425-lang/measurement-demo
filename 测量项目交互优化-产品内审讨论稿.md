# 测量项目交互优化产品内审讨论稿

> 本文用于产品内部讨论，不作为最终 PRD。当前重点是说明体态体围、肩部、颈部三个测量项目为什么要优化、优化了哪些交互点，以及如何通过 Demo 走查方案。

## 一、材料定位

本次方案不是 UI 视觉定稿，也不是完整开发需求。当前阶段主要验证三类设备端测量流程的交互逻辑：

- 用户是否能明确知道当前要做什么。
- 用户是否能知道自己是否已经进入合格姿态或成功状态。
- 用户是否能理解倒计时后即将发生什么。
- 用户是否能顺畅完成连续测量，不被重复校准、重复倒计时或模糊中间页打断。

现有可交互 Demo：

`C:\Users\小林同学\Documents\设备交互流程优化\measurement-demo.html`

Demo 只展示优化后的流程，不展示完整原版对照。后续 UI 视觉可以基于当前流程再单独细化。

---

## 二、体态体围测量

### 1. 原交互现状

- 用户进入体态评估中的体围测量后，主要看静态 3D 人偶和文字提示完成 A-Pose。
- 原文案大意是提示用户双手握拳、自然打开 30 度、保持静止。
- 用户完成准备后进入倒计时，倒计时结束后进行 360 度旋转扫描。

### 2. 用户核心困惑

- “打开 30 度”对普通用户过于抽象，用户很难判断自己手臂是否摆对。
- 用户不知道身体和手臂是否已经处于设备认可的位置，只能靠猜。
- 倒计时页如果只说“请保持静止”，用户不知道倒计时结束后会进入旋转扫描。
- 摆姿阶段、倒计时阶段、扫描阶段都在强调静止，但每个阶段的目标不同，容易混在一起。

### 3. 优化项

- 增加 A-Pose 人形线框，让用户按线框摆姿，而不是理解抽象角度。
- 增加实时 2D 相机画面占位，让用户理解“自己需要进入框内并对齐”。
- 双臂区域做高亮，用视觉区域表达 30 度要求，降低文案理解成本。
- 对齐通过后边框变蓝，给用户明确的合格反馈。
- 对齐通过后提示“姿势正确，请保持 2 秒”，让用户知道当前不是继续调整，而是保持。
- 倒计时文案改为“倒计时结束后将开启旋转扫描，请全程保持身体和头部静止。”，提前告知下一阶段。
- 扫描页关闭右上示范窗，避免扫描阶段继续看摆姿示范造成干扰。
- 扫描页显示 360 度环和扫描线，强化“正在采集”的状态感。
- 等待结果页单独呈现，避免用户误以为扫描结束后可以立刻离开。

### 4. 优化后流程

`APOSE_ALIGN → APOSE_ALIGN_PASS → COUNTDOWN_3 → COUNTDOWN_2 → COUNTDOWN_1 → SCAN_360 → WAIT_RESULT → DONE`

对应说明：

- `APOSE_ALIGN`：A-Pose 对齐，灰色人形线框。
- `APOSE_ALIGN_PASS`：对齐通过，边框变蓝，并提示保持 2 秒。
- `COUNTDOWN_3/2/1`：扫描前倒计时，强调倒计时结束后将开启旋转扫描。
- `SCAN_360`：360 度旋转扫描中。
- `WAIT_RESULT`：等待测量结果。
- `DONE`：测量完成，占位展示结果已生成。

### 5. Demo 走查方式

从项目选择页点击 `体态体围`：

1. 先看 A-Pose 灰色线框和右上示范窗。
2. 点击下一步，模拟对齐成功，边框变蓝。
3. 继续下一步，查看 3、2、1 倒计时文案。
4. 进入 360 度扫描页，确认右上示范窗已关闭。
5. 查看等待结果页和完成页。

### 6. 待产品确认

- 算法是否能支持“边框变蓝”这种明确通过态。
- 是否需要真实相机画面叠加，还是本期先做线框提示。
- “请双手握拳”是否需要作为第二条辅助文案保留。
- 对齐通过后的“保持 2 秒”是否需要真实进度环或仅文案提示。
- 体围扫描结束后是否一定需要等待结果页。

---

## 三、肩部测量

### 1. 原交互现状

- 外展上举和前屈上举是两个独立测量入口。
- 每个入口都有自己的校准、倒计时、左手测量、右手测量。
- 左右手之间存在“请面向屏幕，保持身体自然站立”一类中间页。
- 未测项目有时显示 `0°`。

### 2. 用户核心困惑

- 用户需要重复经历校准和倒计时，流程显得割裂。
- 左手测完后，用户不知道要继续保持、放下，还是准备右手。
- 外展结束后进入前屈的阶段边界不清楚。
- 中间页只说自然站立，没有明确说明用户当前应该放下手还是准备下一步。
- `0°` 容易被理解成已经测出了 0 度，而不是还没测。

### 3. 优化项

- 将外展上举和前屈上举合并为一次“肩部合并测量”。
- 全流程只做一次校准和一次倒计时。
- 四个子项固定顺序：外展左、外展右、前屈左、前屈右。
- 顶部从原来的双格进度改为四格进度。
- 当前步骤使用 `●>>` 高亮。
- 显示 `1/4、2/4、3/4、4/4`，让用户知道当前测到第几步。
- 未测值统一显示 `--`，不再显示 `0°`。
- 每一步动作文案补充“掌心朝内”和“并停止 2 秒”。
- 每侧成功后明确提示“很好，请缓慢放下左手/右手”。
- 外展完成后增加过渡页：“外展上举测量已完成。接下来进入前屈上举测量，请将双臂自然下垂。”
- 保留原设备里的 3D 人偶、角度线、绿色成功勾等视觉语言，降低改造成本。

### 4. 优化后流程

`SH_CALIB → SH_CD3 → SH_CD2 → SH_CD1 → SH_M1 → SH_OK1 → SH_M2 → SH_OK2 → SH_TRANS → SH_M3 → SH_OK3 → SH_M4 → SH_OK4 → SH_DONE`

对应说明：

- `SH_CALIB`：肩部校准。
- `SH_CD3/2/1`：倒计时。
- `SH_M1/OK1`：外展上举左手测量与成功放下。
- `SH_M2/OK2`：外展上举右手测量与成功放下。
- `SH_TRANS`：外展到前屈的过渡。
- `SH_M3/OK3`：前屈上举左手测量与成功放下。
- `SH_M4/OK4`：前屈上举右手测量与成功放下。
- `SH_DONE`：肩部测量完成。

### 5. Demo 走查方式

从项目选择页点击 `肩部功能`：

1. 查看校准页文案：请面向屏幕，双脚站稳，双臂自然下垂。
2. 查看倒计时页文案：请保持身体静止；倒计时结束后将测量外展上举-左。
3. 依次查看外展左、外展右、前屈左、前屈右四步。
4. 重点看每次成功后是否明确提示放下左手或右手。
5. 重点看外展到前屈之间是否有过渡页。
6. 查看完成页是否汇总四项结果。

### 6. 待产品确认

- 四步连续测量是否符合设备采集能力。
- 外展到前屈之间是否需要强制等待用户双臂放下，还是仅提示即可。
- 成功提示停留时长是否需要设定。
- “掌心朝内”是否适用于外展和前屈所有动作。
- 四格进度是否会在实际设备屏幕上过挤，后续 UI 是否需要压缩文案。

---

## 四、颈部测量

### 1. 原交互现状

- 颈椎侧屈和颈椎旋转是两个独立测量流程。
- 每个流程各自倒计时，并测左右两个方向。
- 测量页主要提示保持肩膀以下静止，以及头部动作方向。

### 2. 用户核心困惑

- 侧屈和旋转分开进入，流程割裂。
- 用户完成一个方向后，不一定知道要先回正再进入下一项。
- 倒计时如果只说保持静止，没有说明接下来测哪一项。
- 侧屈结束到旋转开始之间缺少过渡说明。
- 用户可能不知道当前是“继续测量中”还是“已经成功，需要回正”。

### 3. 优化项

- 将侧屈和旋转合并为一次“颈部合并测量”。
- 全流程只做一次校准和一次倒计时。
- 四个子项固定顺序：左侧屈、右侧屈、左旋转、右旋转。
- 顶部四格展示当前和后续项目。
- 当前步骤使用 `●>>` 高亮。
- 显示 `1/4、2/4、3/4、4/4`。
- 校准文案调整为“请平视前方，肩膀以下部位保持静止。”
- 倒计时文案调整为“请保持身体和头部静止；倒计时结束后将测量颈椎左侧屈。”
- 每个测量动作明确“到最大角度，并停止 2 秒”。
- 每次成功后提示“很好，请缓慢将头部回至正中。”
- 侧屈完成后增加过渡页：“侧屈测量已完成。接下来：颈椎旋转；请先将头部回至正中。”
- 保留原颈部 3D 头颈模型、角度弧线、蓝色高亮框风格。

### 4. 优化后流程

`NK_CALIB → NK_CD3 → NK_CD2 → NK_CD1 → NK_M1 → NK_OK1 → NK_M2 → NK_OK2 → NK_TRANS → NK_M3 → NK_OK3 → NK_M4 → NK_OK4 → NK_DONE`

对应说明：

- `NK_CALIB`：颈部校准。
- `NK_CD3/2/1`：倒计时。
- `NK_M1/OK1`：左侧屈测量与回正提示。
- `NK_M2/OK2`：右侧屈测量与回正提示。
- `NK_TRANS`：侧屈到旋转的过渡。
- `NK_M3/OK3`：左旋转测量与回正提示。
- `NK_M4/OK4`：右旋转测量与回正提示。
- `NK_DONE`：颈部测量完成。

### 5. Demo 走查方式

从项目选择页点击 `颈部功能`：

1. 查看校准页文案：请平视前方，肩膀以下部位保持静止。
2. 查看倒计时页文案：请保持身体和头部静止；倒计时结束后将测量颈椎左侧屈。
3. 依次查看左侧屈、右侧屈、左旋转、右旋转四步。
4. 重点看每次成功后是否明确提示头部回正。
5. 重点看侧屈到旋转之间是否有过渡页。
6. 查看完成页是否汇总四项结果。

### 6. 待产品确认

- 头部回正是否需要算法判定，还是只做提示。
- 侧屈到旋转的过渡是否需要用户确认。
- “肩膀以下部位保持静止”是否比“身体静止”更符合产品表达。
- 旋转动作是否需要写“左后方/右后方”，还是改为更通俗表达。
- 四格进度在颈部页面上是否足够清晰。

---

## 五、产品内审建议看点

### 1. 问题判断是否准确

- 三个项目各自的用户困惑是否真实存在。
- 哪些问题是 P0 必须解决，哪些只是体验增强。
- 是否还有比当前方案更关键的阻塞点。

### 2. 流程顺序是否可实现

- 体围是否能支持 A-Pose 线框对齐和蓝框通过态。
- 肩部是否能支持四步连续测量。
- 颈部是否能支持侧屈和旋转合并测量。
- 各项目是否都允许只做一次校准和一次倒计时。

### 3. 文案是否清楚

- 文案是否让用户知道“现在该做什么”。
- 文案是否过长，是否适合设备端阅读。
- “放下手”“头部回正”“倒计时后开始扫描”等关键提示是否需要保留。

### 4. 本期范围是否合理

- 本期是否继续不做失败原因细化。
- 本期是否继续不做重测流程。
- 本期是否只验证交互逻辑，UI 视觉后续单独优化。

---

## 六、当前结论与未决项

当前方案倾向：

- 三个项目均采用“优化后流程优先”的方式验证，不做完整原版 Demo。
- 体围重点解决 A-Pose 摆姿可理解性和扫描前预期。
- 肩部重点解决重复进入、重复倒计时、左右和动作类型之间边界不清。
- 颈部重点解决合并流程、回正提示和侧屈到旋转的过渡。

仍需产品内审确认：

- 哪些优化进入本期开发范围。
- 哪些需要算法支持后才能落地。
- 哪些只是 Demo 表达，后续 UI 设计需要重新绘制。
- 是否需要补充失败提示和重测流程的二期方案。

