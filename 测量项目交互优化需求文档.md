# 测量项目交互优化需求文档

版本：讨论稿  
范围：体态体围测量、肩部测量、颈部测量  
Demo 文件：`measurement-demo.html`

## 1. 文档目标

本文档用于描述当前 Demo 中三个测量项目的优化后交互逻辑与页面文案，便于产品、算法、UI 与设备侧共同评审。

本轮需求重点是验证测量流程是否清晰，暂不作为最终 UI 视觉规范。真实相机画面、模型识别、算法判定、报告页内容等能力可在后续版本继续细化。

## 2. 项目选择页

### 2.1 页面入口

项目选择页包含三个测量入口：

- 体态体围
- 肩部功能
- 颈部功能

### 2.2 交互规则

- 点击 `体态体围`：进入体态体围测量流程。
- 点击 `肩部功能`：进入肩部合并测量流程。
- 点击 `颈部功能`：进入颈部合并测量流程。
- 测量页左上角 `← 项目选择`：返回项目选择页。
- Demo 底部保留 `上一步 / 下一步 / 重置` 控制，用于模拟设备状态流转。

## 3. 体态体围测量

### 3.1 优化目标

体态体围测量的核心目标是让用户明确：

- 开始测量前需要如何摆放双臂。
- 设备正在判断姿势是否对齐。
- 姿势对齐后不能放下手。
- 倒计时结束后转台会开始旋转扫描。
- 旋转扫描过程中仍需保持双臂、身体和头部稳定。

### 3.2 流程状态

体围流程状态顺序如下：

`APOSE_ALIGN → APOSE_PARTIAL → APOSE_READY_3 → APOSE_READY_2 → APOSE_READY_1 → SCAN_360 → WAIT_RESULT → DONE`

### 3.3 页面逻辑与文案

#### 3.3.1 姿势对齐中：初始对齐

状态 ID：`APOSE_ALIGN`

页面表现：

- 页面副标题显示：`姿势对齐中`
- 中央展示标准 A-Pose 公模。
- 公模双肩位置作为红色虚线起点。
- 红色虚线竖直显示，用于提示用户当前双臂仍需向外调整。
- 不显示右上角示范图。

底部文案：

- `双手握拳，手臂向身体两侧打开；`
- `请调整双臂角度，尽量贴近屏幕中的姿势。`

交互逻辑：

- Demo 中点击 `下一步` 进入“姿势对齐中：动态调整”状态。
- 实际设备中应由算法判断用户双臂是否逐步接近目标姿势。

#### 3.3.2 姿势对齐中：动态调整

状态 ID：`APOSE_PARTIAL`

页面表现：

- 页面副标题仍显示：`姿势对齐中`
- 中央展示标准 A-Pose 公模。
- 红色虚线从肩点开始，动态向外展开至贴近模型手臂方向。
- 动画结束后，在模型左右两侧显示绿色对号。
- 不显示右上角示范图。

底部文案：

- `双手握拳，手臂向身体两侧打开；`
- `请调整双臂角度，尽量贴近屏幕中的姿势。`

交互逻辑：

- Demo 中点击 `下一步` 进入倒计时状态。
- 实际设备中可用于表达“姿势正在接近正确位置”或“姿势已被识别为可进入倒计时”。

#### 3.3.3 姿势已对齐：倒计时

状态 ID：`APOSE_READY_3 / APOSE_READY_2 / APOSE_READY_1`

页面表现：

- 页面副标题显示：`姿势已对齐`
- 中央展示 A-Pose 公模。
- 页面上方显示大号倒计时数字 `3 / 2 / 1`。
- 倒计时页不显示红色虚线。
- 倒计时页不显示右上角示范图。

底部文案：

- `姿势正确，请保持双臂位置不变；`
- `倒计时结束后转台将开始旋转扫描。`

交互逻辑：

- Demo 中每点击一次 `下一步`，倒计时从 `3` 切换到 `2`，再切换到 `1`。
- 倒计时阶段保持在同一视觉页面，不切换到独立倒计时页，避免用户误以为已经可以放下手。

#### 3.3.4 旋转扫描中

状态 ID：`SCAN_360`

页面表现：

- 页面副标题显示：`旋转扫描中`
- 中央展示 360° 旋转扫描模型图。
- 模型图中包含旋转方向与扫描感提示。

底部文案：

- `测量中，请保持双臂位置不变，身体和头部不要移动。`

交互逻辑：

- Demo 中点击 `下一步` 进入生成结果状态。
- 实际设备中此阶段对应转台旋转扫描采集过程。

#### 3.3.5 生成结果中

状态 ID：`WAIT_RESULT`

页面表现：

- 页面副标题显示：`生成结果中`
- 页面只展示等待状态，不显示人体模型或扫描效果。

页面文案：

- `请等待测量结果…`

交互逻辑：

- Demo 中点击 `下一步` 进入完成状态。
- 实际设备中此阶段对应数据计算、报告生成或结果同步。

#### 3.3.6 已完成

状态 ID：`DONE`

页面表现：

- 页面显示测量完成状态。
- 展示结果生成占位。

页面文案：

- `测量结果已生成`
- 按钮占位：`查看报告`、`返回`

## 4. 肩部测量

### 4.1 优化目标

肩部测量将原本分散的外展上举、前屈上举流程合并为一次连续测量，减少重复校准和倒计时，并让用户明确当前正在测量第几项、下一步该做什么。

### 4.2 流程状态

肩部流程状态顺序如下：

`SH_CALIB → SH_CD3 → SH_CD2 → SH_CD1 → SH_M1 → SH_OK1 → SH_M2 → SH_OK2 → SH_TRANS → SH_M3 → SH_OK3 → SH_M4 → SH_OK4 → SH_DONE`

四个测量子项固定顺序：

1. 外展上举-左
2. 外展上举-右
3. 前屈上举-左
4. 前屈上举-右

### 4.3 页面逻辑与文案

#### 4.3.1 校准中

状态 ID：`SH_CALIB`

文案：

- `请面向屏幕，双脚站稳，双臂自然下垂。`

#### 4.3.2 即将开始

状态 ID：`SH_CD3 / SH_CD2 / SH_CD1`

文案：

- `请保持身体静止；倒计时结束后将测量外展上举-左。`

#### 4.3.3 外展上举-左

状态 ID：`SH_M1`

文案：

- `左手向左伸直，掌心朝内，缓慢展开上举至最高处，并停止2秒。`

成功状态 ID：`SH_OK1`

成功文案：

- `很好，请缓慢放下左手。`

#### 4.3.4 外展上举-右

状态 ID：`SH_M2`

文案：

- `右手向右伸直，掌心朝内，缓慢展开上举至最高处，并停止2秒。`

成功状态 ID：`SH_OK2`

成功文案：

- `很好，请缓慢放下右手。`

#### 4.3.5 切换到前屈上举

状态 ID：`SH_TRANS`

文案：

- `外展上举测量已完成。接下来进入前屈上举测量，请将双臂自然下垂。`

#### 4.3.6 前屈上举-左

状态 ID：`SH_M3`

文案：

- `左手向前伸直，掌心朝内，缓慢展开上举至最高处，并停止2秒。`

成功状态 ID：`SH_OK3`

成功文案：

- `很好，请缓慢放下左手。`

#### 4.3.7 前屈上举-右

状态 ID：`SH_M4`

文案：

- `右手向前伸直，掌心朝内，缓慢展开上举至最高处，并停止2秒。`

成功状态 ID：`SH_OK4`

成功文案：

- `很好，请缓慢放下右手。`

#### 4.3.8 已完成

状态 ID：`SH_DONE`

页面显示肩部测量完成状态。

### 4.4 展示规则

- 顶部展示四个测量子项。
- 当前测量项高亮。
- 已测量项展示结果值。
- 未测量项使用 `--`，不显示 `0°`。
- 每个动作完成后展示成功反馈，并提示用户缓慢放下手。

## 5. 颈部测量

### 5.1 优化目标

颈部测量将侧屈与旋转合并为一次连续测量，减少重复进入流程的成本，并在每次动作结束后明确提示用户将头部回正。

### 5.2 流程状态

颈部流程状态顺序如下：

`NK_CALIB → NK_CD3 → NK_CD2 → NK_CD1 → NK_M1 → NK_OK1 → NK_M2 → NK_OK2 → NK_TRANS → NK_M3 → NK_OK3 → NK_M4 → NK_OK4 → NK_DONE`

四个测量子项固定顺序：

1. 颈椎左侧屈
2. 颈椎右侧屈
3. 颈椎左旋转
4. 颈椎右旋转

### 5.3 页面逻辑与文案

#### 5.3.1 校准中

状态 ID：`NK_CALIB`

文案：

- `请平视前方，肩膀以下部位保持静止。`

#### 5.3.2 即将开始

状态 ID：`NK_CD3 / NK_CD2 / NK_CD1`

文案：

- `请保持身体和头部静止；倒计时结束后将测量颈椎左侧屈。`

#### 5.3.3 颈椎左侧屈

状态 ID：`NK_M1`

文案：

- `保持肩膀以下身体部位静止`
- `头部向左侧屈到最大角度，并停止2秒`

成功状态 ID：`NK_OK1`

成功文案：

- `很好，请缓慢将头部回至正中。`

#### 5.3.4 颈椎右侧屈

状态 ID：`NK_M2`

文案：

- `保持肩膀以下身体部位静止`
- `头部向右侧屈到最大角度，并停止2秒`

成功状态 ID：`NK_OK2`

成功文案：

- `很好，请缓慢将头部回至正中。`

#### 5.3.5 切换到颈椎旋转

状态 ID：`NK_TRANS`

文案：

- `侧屈测量已完成。接下来：颈椎旋转；请先将头部回至正中。`

#### 5.3.6 颈椎左旋转

状态 ID：`NK_M3`

文案：

- `保持肩膀以下身体部位静止`
- `头部向左后方旋转到最大角度，并停止2秒`

成功状态 ID：`NK_OK3`

成功文案：

- `很好，请缓慢将头部回至正中。`

#### 5.3.7 颈椎右旋转

状态 ID：`NK_M4`

文案：

- `保持肩膀以下身体部位静止`
- `头部向右后方旋转到最大角度，并停止2秒`

成功状态 ID：`NK_OK4`

成功文案：

- `很好，请缓慢将头部回至正中。`

#### 5.3.8 已完成

状态 ID：`NK_DONE`

页面显示颈部测量完成状态。

### 5.4 展示规则

- 顶部展示四个测量子项。
- 当前测量项高亮。
- 已测量项展示结果值。
- 未测量项使用 `--`。
- 每个方向测量成功后，明确提示用户将头部回至正中。

## 6. Demo 控制与评审说明

### 6.1 Demo 控制

- `下一步`：模拟进入下一状态。
- `上一步`：回到上一状态。
- `重置`：回到当前测量流程的第一步。
- `← 项目选择`：返回测量项目入口页。

### 6.2 内审重点

产品评审时建议重点确认：

- 三个项目的流程顺序是否符合设备实际采集能力。
- 体态体围中“姿势对齐中 → 姿势已对齐 → 旋转扫描中”的状态边界是否清晰。
- 体态体围中双臂提示文案是否足够用户化，是否还需要进一步口语化。
- 肩部与颈部的合并测量是否会给设备采集、算法判断或用户体力带来额外压力。
- 成功提示页停留时间、倒计时时长、等待结果时长是否需要产品定义。
- 本期是否需要补充失败、重测、超时、姿势丢失等异常流程。

## 7. 暂不包含内容

本需求文档暂不定义：

- 最终 UI 视觉规范。
- 真实相机画面接入方式。
- 姿势识别算法阈值。
- 测量失败原因分类。
- 报告页内容与数据解释。
- 设备端真实倒计时、扫描时长与接口协议。

